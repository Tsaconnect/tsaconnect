{
  "name": "tsa-smart-contracts",
  "version": "1.0.0",
  "description": "TSA Connect marketplace smart contracts",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.28.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^16.4.5",
    "solc": "^0.8.20"
  }
}
